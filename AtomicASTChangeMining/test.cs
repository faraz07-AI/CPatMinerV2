using System;

namespace HelloWorld
{
    class Hello {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!"+"lol");
            short sample = (short)((buffer[index + 1] << 8) | buffer[index >> 0] >>> 2);
            int a  = 5 & 10 ;
            if ( (a >=2) && (a<5) || (2!=5))
                a ++;

        }
    }
}